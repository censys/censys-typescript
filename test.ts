import { components } from "./schema";

type APIResponse = components["schemas"]["ApiResponse"];

type ApiErrorResponse = components["schemas"]["ApiErrorResponse"];

type Host = components["schemas"]["Host"];



// packages: one for censys search and one for censys asm 
// add those commands to dev dependencies with yarn 

